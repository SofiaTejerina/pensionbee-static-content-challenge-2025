{
  "env": {
    "NODE_ENV": "production"
  },
  "installCommand": "npm install --omit=dev",
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".next"
      }
    }
  ]
}